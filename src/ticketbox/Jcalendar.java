package ticketbox;

public class Jcalendar {
	/*
	 * private JFrame frame;
	 * 
	 * 
	 * public static void main(String[] args) { EventQueue.invokeLater(new
	 * Runnable() { public void run() { try { Jcalendar window = new Jcalendar();
	 * window.frame.setVisible(true); } catch (Exception e) { e.printStackTrace(); }
	 * } }); }
	 * 
	 * 
	 * public Jcalendar() { initialize(); }
	 * 
	 * 
	 * private void initialize() { frame = new JFrame(); frame.setSize(210,210);
	 * frame.setVisible(true);
	 * 
	 * JPanel panel = new JPanel(); frame.getContentPane().add(panel,
	 * BorderLayout.CENTER); panel.setLayout(new BorderLayout(0, 0));
	 * 
	 * 
	 * JCalendar calendar = new JCalendar(); panel.add(calendar,
	 * BorderLayout.CENTER);
	 * 
	 * 
	 * 
	 * }
	 */
}
